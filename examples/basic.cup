let a = 23;
let b = 46;

log(a, b);

fn add(a, b) {
    return a + b;
}

log(add(a, b));

# Types
1. of fn -> Infered from return type
2. of variables -> Infered from assignment, default is `null` (let a;)
3. of parameters -> Infered from assignment, default is `null` (fn add(a, b) { ... })